# prepare for cloning
# run this in the original MW14 -machine, where MYSQL-query logging is turned on
#
# generate a ssh-key which can access this original server
# this gets automatically replicated in the parallel universe, so key-exchanges are not needed
# TODO!